# Outputs Directory

Generated analysis outputs are not committed to Git.

The official reproducibility workflow writes CSV tables to `outputs/tables/`.
Optional lead-lag composite figures are written to `outputs/figures/`.
